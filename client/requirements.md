## Packages
framer-motion | Smooth animations for page transitions and form interactions
react-hook-form | Form state management and validation
@hookform/resolvers | Zod integration for react-hook-form
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
API endpoints:
- POST /api/applications (Create application)
- GET /api/applications (List applications - admin only, optional)

Schema uses:
- insertTutorApplicationSchema for validation
- tutorApplications for types
